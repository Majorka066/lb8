# Лабораторная работа №8. Введение в React + JSX

Простое и функциональное приложение для управления задачами, созданное с использованием React.

## 📸 Скриншоты приложения

<img width="1492" height="719" alt="image" src="https://github.com/user-attachments/assets/2ce23521-0c8d-4c64-9f5a-f5a33b02724c" />


<img width="1491" height="719" alt="image" src="https://github.com/user-attachments/assets/b6ef850e-4a38-44dc-b847-f29551a695e0" />


## ✨ Возможности

- ➕ Добавление новых задач
- ❌ Удаление отдельных задач
- 🗑️ Очистка всего списка задач одной кнопкой
- 💾 Автоматическое сохранение в localStorage
- 🎨 Удобный и понятный интерфейс

## 📚 Основные концепции React

### Что такое компонент?
**Компонент** — это независимый, переиспользуемый блок кода, который возвращает React-элементы для отображения на странице. Компоненты позволяют разбить UI на изолированные части. В нашем проекте есть 4 компонента:
- `App.jsx` — главный компонент, управляющий состоянием всего приложения
- `AddTaskForm.jsx` — компонент формы для добавления задач
- `ToDoList.jsx` — компонент списка задач
- `ToDoItem.jsx` — компонент отдельной задачи

### Что такое пропсы (props)?
**Пропсы** — это параметры, которые передаются от родительского компонента к дочернему. Они позволяют компонентам обмениваться данными. Например:
```jsx
<AddTaskForm addTask={addTask} />
<ToDoList tasks={tasks} removeTask={removeTask} />
```
Здесь функции `addTask` и `removeTask`, а также массив `tasks` передаются как пропсы в дочерние компоненты.

### Что такое состояние (state)?
**Состояние** — это данные, которые компонент хранит и может изменять. При изменении состояния компонент автоматически перерисовывается. В нашем приложении состояние используется для хранения массива задач:
```jsx
const [tasks, setTasks] = useState(() => {
  const saved = localStorage.getItem('tasks');
  return saved ? JSON.parse(saved) : [];
});
```

## 🤖 Использование ИИ в разработке

**Какую часть кода помог написать ИИ:**
- Структуру всех компонентов (`App.jsx`, `AddTaskForm.jsx`, `ToDoList.jsx`, `ToDoItem.jsx`)
- Логику работы с `useState` и `useEffect` для управления состоянием
- Реализацию сохранения данных в `localStorage` с автоматической загрузкой при запуске
- Функционал добавления, удаления и очистки задач
- Условный рендеринг кнопки "Очистить всё" (показывается только при наличии задач)
- Базовую структуру CSS для стилизации компонентов

ИИ предоставил готовые шаблоны кода и помог оптимизировать логику работы приложения.

## 💡 Понимание компонентного подхода

**Что я понял о компонентном подходе:**

1. **Модульность** — каждый компонент отвечает за свою конкретную задачу. `ToDoItem` только отображает задачу, `AddTaskForm` только добавляет её, а `App` координирует всё вместе.

2. **Переиспользуемость** — компонент `ToDoItem` используется для каждой задачи в списке через метод `.map()`, не дублируя код.

3. **Однонаправленный поток данных** — данные передаются от родителя к детям через пропсы, а дочерние компоненты могут вызывать функции родителя для изменения состояния. Это создаёт понятную и предсказуемую архитектуру.

4. **Разделение ответственности** — логика (состояние и функции) находится в `App.jsx`, а представление (UI) разбито на отдельные компоненты. Это делает код более организованным.

5. **Простота поддержки** — если нужно изменить внешний вид кнопки удаления, я редактирую только `ToDoItem.jsx`, не трогая остальной код. Изменения в одном компоненте не влияют на другие.

6. **Читаемость кода** — вместо одного большого файла с множеством функций, у меня есть несколько маленьких компонентов с понятными названиями и чёткой структурой.

Компонентный подход делает код более организованным, понятным и удобным для масштабирования.

## 🚀 Запуск проекта

### Установка зависимостей:
```bash
npm install
```

### Запуск в режиме разработки:
```bash
npm start
```

## 🛠️ Технологии

- **React** — библиотека для создания пользовательских интерфейсов
- **React Hooks** — useState, useEffect для управления состоянием
- **localStorage API** — для сохранения задач в браузере
- **CSS3** — для стилизации компонентов

## 📂 Структура проекта
```
src/
├── App.jsx          # Главный компонент (управление состоянием)
├── AddTaskForm.jsx  # Форма добавления задач
├── ToDoList.jsx     # Список задач
├── ToDoItem.jsx     # Отдельная задача
└── style.css        # Стили приложения
```

## 👨‍💻 Автор

Создано в рамках лабораторной работы №8 по изучению React

## 📝 Выводы

Лабораторная работа помогла понять основы React и преимущества компонентного подхода. Использование состояния и пропсов позволяет создавать интерактивные приложения с чистой и понятной архитектурой. ИИ значительно ускорил разработку, предоставив готовые решения, но понимание принципов работы React осталось ключевым для успешного выполнения задания.
